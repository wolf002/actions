name: eightth workflow
on:
  workflow_dispatch:
    inputs:
      titleCustom:
        description: "Title of the issue"
        required: true
        default: "New issue"
        type: string
jobs:
  run-first-commnads:
    runs-on: ubuntu-latest
    steps:
      - name: Event vars
        run: |
          echo "name: sixth workflow"
          echo $GITHUB_SHA
          echo $GITHUB_REF
