name: ten workflow
on: [push]

jobs:
  run-ten-commnads:
    runs-on: ubuntu-latest
    steps:
      - name: Event vars
        id: "Context-amd-Expression"
        run: |
          echo "vars amd Expression: ${{ toJson(vars) }}"
          echo "github: ${{ toJson(github) }}"
          echo "runner: ${{ toJson(runner) }}"
          echo "job: ${{ toJson(job) }}"
          echo "steps: ${{ toJson(steps) }}"
