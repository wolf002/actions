name: fifth workflow
on: [push, issues, pull_request]
jobs:
  run-first-commnads:
    runs-on: ubuntu-latest
    steps:
      - name: Event vars
        run: |
          echo "name: fifth workflow"
          echo $GITHUB_SHA
          echo $GITHUB_REF
